version: '3'

services:
  frontend:
    image: devopsgroup10projectdev/frontend
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - 81:80
    restart: unless-stopped
    networks:
      - gymcheck

  api:
    image: devopsgroup10projectdev/api
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - 82:8000
    restart: unless-stopped
    environment:
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_HOST=${MYSQL_HOST}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - PORT=${API_PORT}
    networks:
      - gymcheck
    

  cms:
    image: devopsgroup10projectdev/cms
    build:
      context: .
      dockerfile: cms/Dockerfile
    ports:
      - 83:80
    restart: unless-stopped
    networks:
      - gymcheck

volumes:
  mysql: {}
  phpmyadmin: {}

networks:
  gymcheck:
    external: true
