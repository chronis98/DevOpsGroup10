import type Category from "@/models/Category";

type Equipment = {
  "id": number,
  "name": string,
  "description": string,
  "imagePath": string,
  "reportsCount": number,
  "status": number}

export default Equipment;
